#ifndef NODE_H
#define NODE_H
#include <iostream>

using namespace std;

/*
File: node header
*/

class Node {
 public:
  Node(int newvalue);
  ~Node();
  int getValue();
  void setNext(Node* newnext);
  Node* getNext();
  int value;
  Node* next;
};
#endif
